# Binary
/reachr
/dist/

# Snapshot / render outputs (fixtures live under testdata/)
/topology.json
/topology.html
/topology.dot

# Exploratory raw dumps (unsanitized: contain account id / tags)
raw.json
*.raw.json

# Test & coverage artifacts
*.out
coverage.*

# OS / editor
.DS_Store
